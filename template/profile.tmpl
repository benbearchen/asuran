<html>
<head>
<title>{{.IP}}'s profile</title>
<style type="text/css">
/* copy from http://www.w3school.com.cn/tiy/t.asp?f=csse_table_fancy */
#profile
  {
  font-family:"Trebuchet MS", Arial, Helvetica, sans-serif;
  border-collapse:collapse;
  }

#profile td, #profile th 
  {
  font-size:1em;
  border:1px solid #98bf21;
  padding:3px 7px 2px 7px;
  min-width:100px;
  }

#profile th 
  {
  font-size:1.1em;
  text-align:left;
  padding-top:5px;
  padding-bottom:4px;
  background-color:#A7C942;
  color:#ffffff;
  }

#profile tr.alt td 
  {
  color:#000000;
  background-color:#EAF2D3;
  }
</style>

<script type="text/javascript">

function appendCmd(cmd) {
  var cmdBox = document.getElementById('CommandBoxId');
  if (cmdBox.value == "" || cmdBox.value.slice(-1) == "\n") {
    cmdBox.value = cmdBox.value + cmd + "\n";
  } else {
    cmdBox.value = cmdBox.value + "\n" + cmd + "\n";
  }
}

function selectOpt(id) {
  var s = document.getElementById(id);
  return s.value;
}

function proxyCmd(act) {
  var u = document.getElementById('ProxyUrl');
  if (u.value == "") {
    alert('proxy 操作的 URL 不能为空');
    return;
  }

  appendCmd('proxy ' + act + ' ' + u.value);
}

function delayCmd(act) {
  var d = document.getElementById('DelayDuration');
  if (d.value == "") {
    alert('delay 操作的时长不能为空');
    return;
  }

  var u = document.getElementById('ProxyUrl');
  if (u.value == "") {
    alert('proxy 操作的 URL 不能为空');
    return;
  }

  appendCmd('delay ' + act + ' ' + d.value + ' ' + u.value);
}

function deleteCmd() {
  var u = document.getElementById('ProxyUrl');
  if (u.value == "") {
    alert('delete 操作的 URL 不能为空');
    return;
  }

  appendCmd('delete ' + u.value);
}

function redirectOpt() {
  var ip = document.getElementById('DomainRedirectIP');
  if (ip.value == "") {
    return "";
  }

  var re=/^(\d{1,2}|1\d\d|2[0-4]\d|25[0-5])(\.(\d{1,2}|1\d\d|2[0-4]\d|25[0-5])){3}$/;
  if (!re.test(ip.value))
  {
    alert("无效 IP（应为四段数字“xxx.xx.xxx.xx”且数字介于 0~255 之间）");
    return;
  }

  return " " + ip.value;
}

function domainCmd(act, opt) {
  if (opt === undefined) {
    return;
  }

  var d = document.getElementById('Domain');
  if (d.value == "") {
    alert('doman 操作的域名不能为空');
    return;
  }

  appendCmd('domain ' + act + ' ' + d.value + opt);
}

</script>
</head>

<body>
<p>配置名字：{{.Name}}</p>
<p>当前作用 IP：{{.IP}}</p>
<p>操作者 IP：{{.Owner}}</p>
<hr/>
<a href="/profile/{{.IP}}/restart" target="_blank" title="清除超时、缓存等">重启客户端状态</a>
&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
<a href="/profile/{{.IP}}/history" target="_blank" title="查看域名查询、HTTP 访问等日志">查看日志</a>
<hr/>
<table id="profile">
<tr>
<th>编辑命令</th>
<th>URL 匹配模式</th>
<th>代理行为</th>
<th>延时设定</th>
</tr>
{{range .Urls}}
<tr{{if .Even}} class="alt"{{end}}>
<td><input type="button" value="修改" onclick="appendCmd('{{.Edit}}');"/>&nbsp;<input type="button" value="删除" onclick="appendCmd('{{.Delete}}');"/></td>
<td style="min-width:500px;">{{.Pattern}}</td>
<td>{{.Action}}</td>
<td>{{.Delay}}</td>
</tr>
{{end}}
</table>
<hr/>

<table id="profile">
<tr>
<th>编辑命令</th>
<th>域名</th>
<th>查询行为</th>
<th>目标 IP</th>
</tr>
{{range .Domains}}
<tr{{if .Even}} class="alt"{{end}}>
<td><input type="button" value="修改" onclick="appendCmd('{{.Edit}}');"/>&nbsp;<input type="button" value="删除" onclick="appendCmd('{{.Delete}}');"/></td>
<td style="min-width:500px;">{{.Domain}}</td>
<td>{{.Action}}</td>
<td>{{.IP}}</td>
</tr>
{{end}}
</table>
<hr/>

<form action="/profile/{{.Path}}" method="post">
<table width="500"><tr>
<td><b>命令：</b></td><td><input type="submit" value="执行命令"/></td><td>（<a href="/profile/{{.Path}}/export" target="_blank">导出上述配置的命令</a>）</td>
</tr>
</table>
<textarea rows="10" cols="80" id="CommandBoxId" name="cmd"></textarea>
</form>


<hr/>

<b>命令导航：</b>（<a href="/profile/commands" target="_blank">命令规范</a>）
<table border="1">
<tr>
<td rowspan="3">proxy</td><td colspan="2"><button type="button" onclick="proxyCmd('default');">[default]</button>: 使用代理</td><td rowspan="6">URL: <br/><input type="text" id="ProxyUrl" size="40" placeholder="domain.com/path/of/page?query=option"/><br/>URL 格式如下：<br/>[domain[:port]]/[path][?query=option]<br/>域名支持通配符<br/>TODO: 支持非 80 端口</td>
</tr>
<tr>
<td colspan="2"><button type="button" onclick="proxyCmd('cache');">cache</button>: 代理并缓存</td>
</tr>
<tr>
<td colspan="2"><button type="button" onclick="proxyCmd('drop '+selectOpt('DropOption'));">drop</button> <select id="DropOption"><option value="404">404</option><option value="502">502</option></select>: 代理但以状态码出错</td>
</tr>
<tr>
<td rowspan="2">delay</td><td><button type="button" onclick="delayCmd('default');">[default]</button>: 每次请求延时</td><td rowspan="2">时长: <input type="text" id="DelayDuration" size="6"/><br/>如: 100ms, 5s, 1m</td>
</tr>
<tr>
<td><button type="button" onclick="delayCmd('drop');">drop</button>: <br/>前“时长”时间内都掉线</td>
</tr>
<tr>
<td colspan="3"><button type="button" onclick="deleteCmd();">delete</button>: 删除 URL 配置</td>
</tr>
</table>

<p>注: 任何对 URL 的代理行为，均要求域名重定向到代理服务器。如域名还没有配置，则会自动添加</p>

<table border="1">
<tr>
<td rowspan="4">domain</td><td colspan="2"><button type="button" onclick="domainCmd('default', '');">[default]</button>: 默认返回正常 IP</td><td rowspan="4">域名: <input type="text" id="Domain"/><br/>支持通配符，如 *.domain.com <br/>特殊地，all 可以操作所有已配置域名</td>
</tr>
<tr>
<td colspan="2"><button type="button" onclick="domainCmd('block', '');">block</button>: 拦截并不返回</td>
</tr>
<tr>
<td><button type="button" onclick="domainCmd('redirect', redirectOpt());">redirect</button>: 重定向到</td><td>IP:<input type="text" id="DomainRedirectIP"/><br/>或: 留空则代理服务器</td>
</tr>
<tr>
<td colspan="2"><button type="button" onclick="domainCmd('delete', '');">delete</button>: 删除域名配置</td>
</tr>
</table>

</body>
</html>
